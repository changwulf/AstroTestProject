{
  "name": "qiaohe-site",
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/copy-images.mjs",
    "preview": "npm run build && wrangler dev",
    "deploy": "npm run build && wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "12",
    "astro": "^5.2.0",
    "marked": "^15.0.0"
  },
  "devDependencies": {
    "wrangler": "^4.110.0"
  }
}
